# Acceptance Coverage Matrix

| Criterion | Semantic source / invariant | Production path | Positive proof | Negative or mutation proof | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Capture no facial images | Saved row has one gesture label plus 126 hand-landmark values only | `src/collect_samples.py`, `src/data_io.py` | Capture writes numeric CSV row | Wrong feature count raises `ValueError` | `tests/test_data_io.py` | verified |
| Capture avoids obvious duplicate poses | Cooldown and landmark-distance gate must accept a varied sample | `src/capture_quality.py`, `src/collect_samples.py` | First and changed poses are accepted | Immediate and near-identical poses are rejected | `tests/test_capture_quality.py` | verified |
| Current camera library is supported | MediaPipe Tasks receives the local hand model in video mode | `src/hand_tracker.py`, `src/setup_assets.py` | Detector starts from official model asset | Missing model gives useful error | Blank-frame runtime test with `models/hand_landmarker.task` | verified |
| Deployment does not require image upload | MediaPipe's model is local, but its package may emit metrics | `README.md` setup and privacy instructions | Run after model download while offline | Network attempt is documented rather than hidden | Local test observed a telemetry connection attempt | gap |
| Heart needs two visible hands | `required_hands("heart") == 2` | `src/gesture_features.py`, `src/collect_samples.py` | Heart capture accepts two hands | Heart capture rejects one hand | `tests/test_gesture_features.py` | verified |
| Feature vectors are portable | Every camera result becomes 126 floats | `src/gesture_features.py` | One/two hands produce 126 values | More than two hands rejects | `tests/test_gesture_features.py` | verified |
| Training creates a portable laptop model | Model is a dependency-light `.npz` file | `src/train.py`, `src/gesture_model.py` | Train saves and reloads model | Missing dataset/model reports a useful error | `tests/test_gesture_model.py`; current model evaluated locally | verified |
| Unknown gestures are not forced into a known class | Nearest distance exceeds trained limit | `src/gesture_model.py` | Nearby sample predicts known label | Distant sample returns `unknown` | `tests/test_gesture_model.py` | verified |
| Casual hands do not trigger a reaction | A live gesture must be close, confident, stable, and use the required hand count | `src/gesture_gate.py`, `src/live_demo.py`, `src/interactive_robot.py` | Stable trained pose activates after the gate delay | Distant, weak-vote, one-hand heart, and released hand remain inactive | `tests/test_gesture_gate.py`, held-out gate check | verified |
| Demo turns heart into a robot reaction | `heart -> heart` | `src/live_demo.py` | Live heart draws blue heart | No hands produces `idle` | Manual camera demo pending runtime | gap |
| Conversation controls only allowed reactions | Provider result has a valid `Reaction` and `Action` | `src/conversation.py`, `src/robot_state.py` | Game/music intents produce expected commands | Invalid Ollama action falls back safely | `tests/test_conversation.py` | verified |
| Local language model is connected without a paid API | Ollama returns validated JSON through the local `localhost` endpoint | `src/conversation.py`, `src/chat_console.py` | `llama3.2:1b` gives a live response with no fallback | Explicit commands bypass LLM ambiguity and remain deterministic | Manual Ollama run plus live provider/game test, 2026-08-25 | verified |
| Robot speaks with a natural local neural voice | Piper synthesizes English speech from the bundled local voice model without a cloud API | `src/speech.py`, `src/voice_demo.py`, `src/interactive_robot.py` | `PiperSpeaker` loads `en_US-lessac-medium` and plays a spoken phrase | Missing voice model raises a useful error; Windows TTS remains an explicit fallback | Manual Piper playback returned `piper_voice_ok=True`, 2026-08-26 | verified |
| Speech delivery matches a trusted robot reaction | The delivery planner maps reaction and punctuation to explicit phrase styles | `src/speech.py`, `src/voice_demo.py`, `src/interactive_robot.py` | Proud and exclamatory phrases use faster, more varied delivery; questions use their own clear style | Blank replies make no audio segments; neutral phrases retain a neutral style | `tests/test_speech.py`; two-phrase Piper playback returned `expressive_piper_ok=True`, 2026-08-26 | verified |
| Full demo gives conversation priority over gestures | Gestures are disabled while the robot listens, thinks, or speaks; hands must leave the camera before the next gesture is accepted | `src/interactive_robot.py`, `src/gesture_gate.py` | Camera loop continues rendering hand and voice state without changing a conversation reaction | A held gesture cannot fire after a conversation ends | `tests/test_interactive_robot.py`, `tests/test_gesture_gate.py`, 2026-08-27 | verified |
| Push-to-talk prevents continuous microphone capture | Pressing Space starts one Vosk turn and releasing it signals the listener to finish | `src/push_to_talk.py`, `src/speech_input.py`, `src/interactive_robot.py` | Held-space reader is testable and listener accepts a release event | No spoken phrase returns to idle without creating a robot reply | `tests/test_push_to_talk.py`, `tests/test_speech_input.py`; live microphone test pending | gap |
| Heart reaction remains noticeable | A detected heart holds the face reaction after hand tracking loses the gesture | `src/interactive_robot.py`, `src/robot_face.py` | Heart stays visible for the configured hold duration | After the hold expires, the current voice state returns | `tests/test_interactive_robot.py` | verified |
| Conversation enters and exits the object game explicitly | Only a game request starts it; a spoken/text stop exits it | `src/robot_runtime.py`, `src/chat_console.py` | Game request produces the first question in the same session | `stop` closes the active game | `tests/test_robot_runtime.py` | verified |
| Object game chooses useful questions and handles uncertainty | Expected information gain selects the next question; five answer likelihoods update the posterior | `src/object_game.py`, `src/robot_runtime.py` | `yes`, `probably`, `maybe`, `probably not`, and `no` are parsed and update scores appropriately | A shared attribute has no information gain; a hesitant answer is weaker than `yes` | `tests/test_object_game.py`, `tests/test_robot_runtime.py` | verified |
| Guess confirmation requires a clear answer | Only a clear affirmative ends a successful guess; uncertain confirmations ask again | `src/robot_runtime.py` | `correct` and `sure` normalize to yes; `probably` keeps the game active | Voice-style negatives and uncertainty do not silently end the game | `tests/test_robot_runtime.py`, 2026-08-27 | verified |
| Object catalog can be solved by the game | Every object has a distinct attribute signature and truthful answers lead to its own top score | `data/object_catalog.json`, `src/object_game.py` | Simulation guesses all 60 catalog objects within 12 questions | Duplicate attribute signatures fail the catalog test | `tests/test_object_game.py`, 2026-08-27 | verified |
| A wrong guess becomes a controlled learning suggestion | A correction asks for the intended object and distinguishing question, then writes a review queue entry | `src/object_learning.py`, `src/robot_runtime.py` | Wrong guess records one JSON line; correct guess ends the game | Empty correction is rejected and no suggestion silently changes the catalog | `tests/test_object_learning.py`, `tests/test_robot_runtime.py`, 2026-08-26 | verified |
| Human QA records only the needed game evidence | Trial logs store normalized answers, result, confidence, and optional question note without audio or participant name | `src/game_trials.py`, `src/game_trial.py` | One local test round can be summarized without reading raw conversation | Invalid non-normalized answers are rejected | `tests/test_game_trials.py`, 2026-08-27 | verified |
| Human trials can calibrate answer likelihoods safely | Per-question probabilities are learned only after enough examples on both catalog-truth sides; default probabilities remain the fallback | `src/object_game_training.py`, `src/train_object_game.py`, `src/object_game.py` | Training writes a calibration and reserved-round comparison; the robot loads it for the next game | Empty or insufficient data calibrates no question; `dry-run` writes nothing | `tests/test_object_game_training.py`, `tests/test_robot_runtime.py`, 2026-08-27 | verified |
| Suggestions require explicit review before promotion | A reviewer can list, reject, or approve a suggestion only with a category, attributes, and a valid question | `src/object_review.py`, `src/review_object_suggestions.py` | Approved suggestions update the catalog and leave the queue; rejected suggestions only leave the queue | Duplicate names, duplicate signatures, and malformed attributes are rejected | `tests/test_object_review.py`, 2026-08-27 | verified |
| Music is chosen from explicit events or requests | Selector has no face/identity input | `src/music.py` | Heart selects warm track | Explicit category overrides game result | `tests/test_music.py` | verified |
| Face reactions are visually distinct and animate | Each reaction has one distinct visual signature; speaking and other dynamic states change over time | `src/robot_face.py`, `src/live_demo.py` | All states draw pixels; fixed-time frames are distinct; speaking frames differ over time | `tests/test_robot_face.py` rejects duplicate state signatures | `tests/test_robot_face.py`, rendered preview | verified |
| Expressions can be tested without a webcam | Preview enumerates every `Reaction` with a subtitle | `src/face_demo.py` | Preview has all reaction states | Missing subtitle or reaction is rejected | `tests/test_face_demo.py`, manual run | verified |
| Public data import stays bounded and identity-free | Importer reads only labeled hand landmarks and limits each class | `src/import_hagrid.py` | Output contains at most requested landmark rows per supported gesture | Metadata and image fields cannot reach the CSV schema | `tests/test_hagrid_import.py` | verified |
