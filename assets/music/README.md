# Music Assets

Place royalty-free or original `.wav` tracks here using the names in
`playlist.json`. The selector is complete, but it does not ship with audio
files so the project never redistributes music without clear permission.

