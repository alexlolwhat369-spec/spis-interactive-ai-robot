mediapipe>=0.10.0,<0.11
numpy>=1.26,<3
# MediaPipe also needs OpenCV's optional modules. Do not install opencv-python
# alongside this package because both distributions provide the cv2 module.
opencv-contrib-python>=4.9,<5
sounddevice>=0.5,<0.6
vosk>=0.3,<0.4
piper-tts>=1.4,<2
pygame>=2.6,<3
# Web UI (camera + robot face in the browser). Standard-library-friendly, no build step.
flask>=3,<4
